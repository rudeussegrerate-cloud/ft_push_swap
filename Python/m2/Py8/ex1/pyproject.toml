[project]
name = "loading"
version = "0.1.0"
description = ""
authors = [
    {name = "None"}
]
requires-python = ">=3.10"
dependencies = [
]

[dependency-groups]
dev = [
    "numpy (>=1.26)",
    "pandas (>=2.1)",
    "matplotlib (>=3.8)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
